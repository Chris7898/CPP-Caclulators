#include <iostream>
#include <cmath>
#include "const.h"

int main(){

    std::cout <<"Enter the radius: ";
    std::cin >> radius;

    result = pi * radius * radius;
    std::cout << "result: " << result << '\n';

  return 0;
}